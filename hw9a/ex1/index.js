const circle = require("./circle.js");
const radius = 3

console.log(`The circumference of a circle with a radius of ${radius} is ${circle.circumference(radius)}`);

console.log(`The area is ${circle.area(radius)}`);